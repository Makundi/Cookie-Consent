# Cookie-Consent
A simple JavaScript project that demonstrates basic DOM manipulation and event handling by creating a cookie consent popup. When a user visits the page, a popup appears with a message about cookie usage and an "Accept" button. Clicking the button hides the popup, providing a straightforward user interaction.
